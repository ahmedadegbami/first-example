import React from 'react'
import ReactDOM from 'react-dom/client'
import './index.css'
import App from './App'

const root = ReactDOM.createRoot(document.getElementById('root'))
root.render(<App />)

// at line 6 we are creating a root element out of an empty div present in public/index.html (our only HTML FILE)

// At line 7, on thet root we are reading our first REACT componenet (APP)
